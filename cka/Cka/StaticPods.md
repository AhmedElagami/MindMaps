# ⚠️ Static Pods (edge-case)

## Content

- Defined by files in `/etc/kubernetes/manifests`.
- Managed directly by the kubelet without the API server.
- Useful for core components on control-plane nodes.
